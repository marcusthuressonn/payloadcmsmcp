# Port the MCP server listens on (Railway sets this automatically).
PORT=8080

# Shared-secret bearer token required on POST /mcp. When set, clients must send
# `Authorization: Bearer <MCP_API_KEY>`. Leave unset only for local development.
# Generate one with: openssl rand -hex 32
MCP_API_KEY=
